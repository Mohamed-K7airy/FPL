# 📋 تقرير الفحص والمراجعة الشاملة للفرونت إند (Frontend Audit Report)
**المشروع:** MINI FPL Platform  
**التاريخ:** 16 أغسطس 2026  
**نطاق الفحص:** ملفات واجهة المستخدم (HTML, React, TypeScript, CSS)، قواعد وبيانات الفانتازي، جداول الفرق، ملفات النصوص والترجمة، ومكونات الإعلانات.

---

## 📑 فهرس التقرير
1. [أخطاء بيانات الفرق وجداول المباريات وقواعد الفانتازي (FPL Logic & Team Bugs)](#1-أخطاء-بيانات-الفرق-وجداول-المباريات-وقواعد-الفانتازي)
2. [الأخطاء الإملائية واللغوية والنحوية (Typographical & Grammatical Errors)](#2-الأخطاء-الإملائية-واللغوية-والنحوية)
3. [أخطاء الواجهات والترجمة وتبديل اللغات (UI & Localization Inconsistencies)](#3-أخطاء-الواجهات-والترجمة-وتبديل-اللغات)
4. [أخطاء إعلانات Google AdSense (AdSense Configuration Errors)](#4-أخطاء-إعلانات-google-adsense)
5. [ملاحظات الأداء والحالات الحدية (Performance & Edge Cases)](#5-ملاحظات-الأداء-والحالات-الحدية)
6. [خطة المعالجة والإصلاح المقترحة](#6-خطة-المعالجة-والإصلاح-المقترحة)

---

## 1. أخطاء بيانات الفرق وجداول المباريات وقواعد الفانتازي

### 🔴 1.1 فرق غير موجودة في البريميرليج وغياب أندية أساسية
* **الملف:** `client/src/components/PitchView.tsx` (السطور 45–97)
* **الوصف:**
  - جدول `TEAM_SCHEDULES` يحتوي على أندية من الدرجة الأولى (Championship) مثل:
    - كوفنتري سيتي (`COV`)
    - هال سيتي (`HUL`)
    - ليدز يونايتد (`LEE`)
    - سندرلاند (`SUN`)
  - في المقابل، تغيب أندية البريميرليج الرسمية:
    - وست هام يونايتد (`WHU`)
    - وولفرهامبتون (`WOL`)
    - ليستر سيتي (`LEI`)
    - ساوثهامبتون (`SOU`)
* **الخلل التتابعي:** في الدالة `getTeamFixtureForGw` (السطر 93):
  ```typescript
  const list = TEAM_SCHEDULES[code] || TEAM_SCHEDULES['ARS'];
  ```
  عند اختيار أي لاعب من وست هام أو وولفرهامبتون أو ليستر سيتي، يتم افتراضياً إظهار جدول مباريات **أرسنال (ARS)** بدلاً من مواجهات فريقه الفعلية.

---

### 🔴 1.2 خطأ في بيانات اللاعب والفريق
* **الملف:** `client/src/data/articles.ts` (السطر 69)
* **الوصف:** كارت اللاعب مكتوب: `playerName: 'أنتونين كينسكي'`, `playerTeam: 'توتنهام'`، ومسار الصورة: `firstteam_profiles_202526_antoninkinsky.webp`.
* **الخلل:** أنتونين كينسكي حارس سلافيا براغ التشيكي وليس حارس توتنهام في الدوري الإنجليزي (حراس توتنهام هم فيكاريو، فورستر، أوستن).
* **اسم المجلد:** المجلد `public/players/Man Unitd` مكتوب بخطأ إملائي في الكلمة (`Unitd` بدلاً من `United`).

---

### 🔴 1.3 خطأ في حساب نقاط الجولة الخام ومضاعف الكابتن
* **الملف:** `client/src/pages/PointsPage.tsx` (السطور 287 و 291)
* **الوصف:**
  1. **السطر 287:** يعرض `p.fullData?.total_points || 0` في خانة النقاط الخام للجولة (Raw Points)، مما يعرض إجمالي نقاط اللاعب التراكمية في الموسم بدلاً من نقاط الجولة الحالية الخام.
  2. **السطر 291:** مضاعف الكابتن مكتوب بشكل ثابت `{p.isCaptain ? '2x' : '1x'}`، دون التحقق من تفعيل خاصية التريبل كابتن (Triple Captain) حيث يجب أن يكون `3x`.

---

### 🔴 1.4 مباريات افتراضية غير متطابقة في نافذة اللاعب (FDR Modal)
* **الملف:** `client/src/components/PlayerDetailModal.tsx` (السطور 40–46)
* **الوصف:** عند عدم ورود مصفوفة `fixtures` للاعب من السيرفر، يرجع الكود إلى مصفوفة ثابتة `defaultFixtures` لجميع لاعبي الدوري (`MUN, ARS, SOU`) بدلاً من استنتاج المواجهات الحقيقية لفريق اللاعب من دالة `getTeamFixtureForGw`.

---

### 🔴 1.5 متغير نقاط الجولة غير معروض (Unrendered Computed Variable)
* **الملف:** `client/src/pages/SquadPage.tsx` (السطور 204–210)
* **الوصف:** يتم حساب المتغير `gwPoints` بدقة مع مضاعفات الكابتن والخواص، لكنه غير مستخدم وغير معروض في الواجهة إطلاقاً.

---

### 🔴 1.6 تسمية مضللة لترتيب الجولة
* **الملف:** `client/src/pages/SquadPage.tsx` (السطور 328–334)
* **الوصف:** الكارت مكتوب عليه `GW Rank / ترتيب الجولة` لكنه يعرض `user?.rank` وهو **الترتيب العام الكلي (Overall Rank)** للمدرب وليس ترتيب الجولة الأسبوعي.

---

## 2. الأخطاء الإملائية واللغوية والنحوية

| # | الملف | السطر | النص الحالي في الكود | التصحيح اللغوي الدقيق | الشرح وسبب الخطأ |
|---|---|---|---|---|---|
| 1 | `articles.ts` | 66 | `تستقبل تصديدات متعددة` | `تستقبل تسديدات متعددة` | خطأ إملائي في كتابة "تسديدات" |
| 2 | `articles.ts` | 84, 86, 99, 100, 101 | `الأظهيرة الهجومية` | `الأظهرة الهجومية` | خطأ لغوي مكرر في جمع كلمة "ظهير" |
| 3 | `articles.ts` | 101 | `مثل مدافعي أرسنال ليفربول` | `مثل مدافعي أرسنال وليفربول` | سقط حرف العطف (الواو) |
| 4 | `articles.ts` | 137 | `عدم تكبيتنه يمثل مخاطرة` | `عدم اختياره كابتناً` / `عدم تكبيله` | خطأ طباعي في الكلمة |
| 5 | `articles.ts` | 138 | `تنبيه هائم:` | `تنبيه هام:` | خطأ إملائي بوضع همزة على ياء |
| 6 | `articles.ts` | 153 | `المتى وكيف تستغل` | `متى وكيف تستغل` | ركاكة لغوية بإدخال "الـ" على "متى" |
| 7 | `articles.ts` | 318 | `نقاط بدلاءك الأربعة` | `نقاط بدلائك الأربعة` | خطأ نحوي (مضاف إليه مجرور، الهمزة على نبرة) |
| 8 | `articles.ts` | 374 | `أندية دوري الأطلس ودوري الأبطال` | `أندية البريميرليج ودوري الأبطال` | كلمة مجهولة ("دوري الأطلس" لا وجود لها) |
| 9 | `HomePage.tsx` | 35, 138 | `تحدَّ أصدقائك` | `تحدَّ أصدقاءَك` | خطأ نحوي (مفعول به منصوب بالفتحة على الهمزة) |
| 10 | `HomePage.tsx` | 47 | `انضم الان` | `انضم الآن` | همزة مد في كلمة "الآن" |
| 11 | `HomePage.tsx` | 130, 135 | `اختار فريقك… بس المرة دي` / `هتلاقي أداة` | `اختر فريقك... لكن هذه المرة` / `ستجد أداة` | خلط بين اللهجة العامية المصرية واللغة الفصحى |
| 12 | `HomePage.tsx` | 369 | `ارتفاع هبوط أسعار اللاعبين` | `ارتفاع وهبوط أسعار اللاعبين` | سقط حرف العطف (الواو) |
| 13 | `GuidesPage.tsx` | 86, 161, 163 | `اختار 5 نجوم` / `اختار تشكيلتك` | `اختر 5 نجوم` / `اختر تشكيلتك` | "اختار" بصيغة الماضي/العامية، والأصل فعل أمر "اختر" |
| 14 | `GuidesPage.tsx` | 171, 174 | `عين الكابتن` | `عيّن الكابتن` | كتابة الفعل بدون شدة |
| 15 | `GuidesPage.tsx` | 348 | `وتحدَّ أصدقائك` | `وتحدَّ أصدقاءَك` | خطأ نحوي (مفعول به منصوب) |
| 16 | `TransfersPage.tsx` | 317 | `تم حفظ التغييرات والتواكيل بنجاح!` | `تم حفظ التشكيلة وتعيين الكابتن بنجاح!` | كلمة "التواكيل" عامية وغير دقيقة للمنصات الرياضية |
| 17 | `TransfersPage.tsx` | 607 | `0 نقلات` | `0 نقلة` أو `لا توجد انتقالات مجانية` | التمييز بعد الأرقام |
| 18 | `PointsPage.tsx` | 253 | `تفاصيل لاعبين الجولة` | `تفاصيل لاعبي الجولة` | خطأ نحوي (تحذف نون جمع المذكر السالم عند الإضافة) |
| 19 | `AdminPage.tsx` | 81 | `هل أنت تأكد من تصفير...` | `هل أنت متأكد من تصفير وإلغاء...` | سقطت الميم في كلمة "متأكد" |
| 20 | `PrivacyPage.tsx` | 44 | `موردي الإعلانات الخارجين` | `موردي الإعلانات الخارجيين` | خطأ إملائي بياء واحدة بدلاً من ياءين |
| 21 | `PrivacyPage.tsx` | 67 | `التواصل الإلكتورني` | `التواصل الإلكتروني` | خطأ طباعي في كلمة "الإلكتروني" |
| 22 | `index.html` | 41, 48, 53 | `تشكيلاتك`, `الأظهيرة`, `الرسمى` | `تشكيلتك`, `الأظهرة`, `الرسمي` | أخطاء إملائية ولغوية في الصفحة الرئيسية |

---

## 3. أخطاء الواجهات والترجمة وتبديل اللغات

1. **انعكاس النص التلميحي (Placeholder Inversion Bug) في صفحة التسجيل:**
   * **الملف:** `client/src/pages/RegisterPage.tsx` (السطر 108)
   * **الكود الحالي:**
     ```tsx
     placeholder={isRtl ? 'Team Name' : 'e.g. Gunners XI / Al-Ahly FC'}
     ```
   * **المشكلة:** عند اختيار العربية (`isRtl = true`) يظهر الـ Placeholder باللغة الإنجليزية `'Team Name'`، وعند الإنجليزية يظهر نص الأمثلة.
   * **التصحيح:**
     ```tsx
     placeholder={isRtl ? 'مثال: أبطال الفانتازي / Gunners XI' : 'e.g. Gunners XI / Al-Ahly FC'}
     ```

2. **غياب الترجمة الإنجليزية الكاملة عن صفحات رئيسية (Hardcoded Arabic):**
   * **`HomePage.tsx`:** جميع الأقسام (العناوين، النصوص، البطاقات، الـ FAQ) مكتوبة باللغة العربية الثابتة ولا تتغير إطلاقاً عند الضغط على زر `English`.
   * **`GuidesPage.tsx`:** الصفحة بالكامل مكتوبة بالعربية فقط دون دعم للغة الإنجليزية.

3. **رسائل تنبيه وتأكيد إنجليزية فقط للمستخدم العربي:**
   * **الملف:** `client/src/pages/LeaguesPage.tsx` (السطر 79)
   * **المشكلة:** رسالة إنشاء الدوري تظهر بالإنجليزية فقط: `League "${data.league.name}" created! Code: ${data.league.code}` بدون مراعاة `isRtl`.

4. **تسمية غير صحيحة لعمود جدول النقاط:**
   * **الملف:** `client/src/pages/PointsPage.tsx` (السطر 261)
   * **المشكلة:** الجدول يستخدم `<th>{t('playerSelection')}</th>` التي تترجم إلى *"تنسيق ونقل اللاعبين"* بدلاً من كلمة *"اللاعب / Player"*.

5. **تسمية غير دقيقة لزر الحفظ في صفحة الانتقالات:**
   * **الملف:** `client/src/pages/TransfersPage.tsx` (السطر 644)
   * **المشكلة:** الزر يظهر دائماً بنص `"إنشاء فريق" / "Create Squad"` حتى عندما يمتلك المستخدم تشكيلة بالفعل ويقوم فقط بتبديل لاعبين أو إجراء انتقالات.

---

## 4. أخطاء إعلانات Google AdSense

* **الملف:** `client/src/pages/ArticlePage.tsx` (السطور 277 و 317)
* **الكود الحالي:**
  ```tsx
  <AdBanner slotId="article-top" format="auto" />
  <AdBanner slotId="article-bottom" format="rectangle" />
  ```
* **سبب الخطأ:** يتم تمرير نصوص أبجدية `"article-top"` و `"article-bottom"` كـ `slotId`، بينما محرك Google AdSense الرسمي يرفض أي معرف غير رقمي (Numeric Ad Slot ID مثل `"1234567890"`). هذا يؤدي إلى ظهور أخطاء AdSense في الـ Console وفشل ظهور الإعلانات.

---

## 5. ملاحظات الأداء والحالات الحدية

1. **تخزين الصور Base64 في LocalStorage:**
   * في `AdminArticlePublisher.tsx` و `articleService.ts`، حفظ المقالات المحتوية على صور Base64 داخل الـ `localStorage` قد يؤدي لتجاوز حد الـ 5MB، لذا يُفضل الاعتماد على ضغط الصور أو استخدام روابط CDN/صور ثابتة.

2. **تحكم اتجاه الصفحة `lang` و `dir` في `index.html`:**
   * السطر 2 في `index.html` يحتوي على `<html lang="ar" dir="rtl">` الثابت. قد يحدث وميض اتجاه (FOUC) للمستخدم الذي اختار اللغة الإنجليزية قبل اكتمال تحميل React.

3. **مفتاح `key` في جدول المتصدرين:**
   * في `LeaguesPage.tsx` (السطر 320): `key={row.userId}` يُفضل تغييره إلى `key={row.userId || row.teamName || idx}` لتجنب أخطاء React List Keys عند وجود مستخدمين تجريبيين.

---

## 6. خطة المعالجة والإصلاح المقترحة

1. **تحديث بيانات الفرق:** تحديث جدول `TEAM_SCHEDULES` وقاموس `TEAM_NAME_TO_SHORT` لإضافة أندية البريميرليج الرسمية وتصحيح جداول المباريات.
2. **التصحيح اللغوي والإملائي:** تطبيق التعديلات المذكورة في جدول الأخطاء الإملائية والنحوية.
3. **إصلاحات الـ i18n:** تصحيح انعكاس الـ placeholder في `RegisterPage.tsx` وإضافة دعم الإنجليزية للصفحة الرئيسية ودليل القواعد.
4. **تصحيح شاشة النقاط:** قراءة نقاط الجولة الخام والمضاعف في `PointsPage.tsx`.
5. **تصحيح معرفات AdSense:** استبدال المعرفات النصية بأرقام صالحة لتفادي انهيار إعلانات Google.
